Awb module specification

%AWB_START

%name Open x86 Micro Inst
%desc Open x86 Micro Instruction
%attributes x86
%provides micro_inst
%requires iaddr 

%public open_x86_micro_inst.h 
%private open_x86_micro_inst.cpp

%param %dynamic STRICT_ORDERING 1 "feeder calls must happen in order"

%param %dynamic LOG2_X86MICRO_UID_BUMP   1 "uids can be incremented by more than one. make inserts easier."

%AWB_END

